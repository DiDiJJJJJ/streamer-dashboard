# 7 月数据导入方案（运营月度统计）

> 生成时间：2026-09-07　适用系统：线下主播管理后台（streamer-dashboard）
> 结论先行：**现有结构能承载 7 月数据，但需要补 3 个记录级字段 + 2 张表**；
> 你选的「整月区间导出」能进系统、能出数，但**无法还原月内运营变更**，本文给出替代方案。

---

## 一、现状评估

### 1.1 数据存量（实测）

| 项 | 值 |
|---|---|
| latest.json 记录数 | 39,549 条 |
| 月份分布 | 2026-08（32,191 条 / 1,045 房间）、2026-09（7,358 条 / 1,052 房间） |
| **2026-07** | **完全没有**（系统最早数据从 8/10 开始） |
| 运营 | 林夕、梧桐、予我、小辫子、轨迹（5 人） |
| 线下名册 | 49 人（roster.offlineRooms，无生效时间） |

8 月既有基数（用于环比对照）：林夕 ¥317,121 / 梧桐 ¥173,162.7 / 予我 ¥120,142.7 / 小辫子 ¥65,200.8 / 轨迹 ¥35,528.9。

### 1.2 现有统计口径（server/operatorStats.js）

- 数据源：`latest.json`（按「房间号::统计日期」累积的按日快照）
- 月份：`统计结束日期` 取 `YYYY-MM`
- 运营：记录自带的 `运营经纪人` 字段（`林夕\|运营组\|虚拟线下部` → 取 `|` 前一段）
- 线下：房间号是否命中 `roster.offlineRooms`（**当前快照，无历史维度**）
- 去重：区间快照（开始日期 ≠ 结束日期）**直接跳过**，防止与每日数据重复计数

### 1.3 三个边界场景的支持度

| 场景 | 支持度 | 依据与风险 |
|---|---|---|
| **月内 / 跨月变更运营** | 按日明细 ✅ 支持<br>整月区间 ❌ 不支持 | 已实测 B 站按日导出保留**当日归属**：房间 `1732562810` 在 8/1~9/3 挂「轨迹」、9/4 起变「小辫子」。但整月区间导出一个主播只有一行、运营取的是导出时刻的当前值，月内换人时会把整月流水算给一个人 |
| **7 月在职、8 月离职** | 流水 ✅ 支持<br>线下划分 ⚠️ 有风险 | 统计**不读** `roster.status`，只看有没有流水记录，所以离职不影响 7 月出数。但若该主播已被移出线下名册，其 7 月流水会被算进**线上** |
| **7 月在后台、之后不在** | 流水 ✅ 支持<br>⚠️ 有误删风险 | 同上。唯一风险是执行「强制移除」会真的从 latest.json 删记录——**不要对历史月份做强制移除** |

---

## 二、需要补充的字段

### 2.1 记录级（导入时固化，写入每条记录）

| 字段 | 取值 | 解决的问题 |
|---|---|---|
| `统计月份` | `2026-07` | 流水明细的归属月份，供统计与对账 |
| `归属运营（快照）` | 归一化运营名，如 `林夕` | 固化导入时的归属，后续主播换运营也不会改写历史月份 |
| `是否线下（快照）` | `是` / `否` | 固化导入时的线下身份，后续被移出名册也不会把 7 月流水算成线上 |

统计时**优先读快照字段**，读不到才回退实时口径（运营经纪人字段 / 当前名册），历史数据因此完全免疫后续状态变化。

### 2.2 新增表

| 文件 | 用途 |
|---|---|
| `server/data/monthly_snapshot.json` | 整月区间数据。**不写进 latest.json**——区间记录会被去重规则跳过（写了也统计不到），还会污染前端看板（今日数据里冒出 7 月的区间行） |
| `server/data/operator_assignment.json` | 主播 × 运营归属关系 + 生效/失效时间：`{ 房间号: [{operator, from, to, source, note}] }`，用于整月数据无法还原变更时按区间覆盖 |

---

## 三、导入方案设计

### 3.1 两种来源自动分流（无需手工选择）

| 形态 | 判定 | 落点 | 去重键 |
|---|---|---|---|
| **按日明细** | `统计开始日期 == 统计结束日期` | `latest.json`（与 8/9 月同库） | `房间号::日期` |
| **整月区间** | `统计开始日期 != 统计结束日期` | `monthly_snapshot.json` | `房间号::月份`（整月覆盖） |

统计融合规则（已实现，不会重复计数）：
- 该月**只有快照** → 用快照（7 月就是这种）
- 该月**已有按日明细** → 用明细；快照里**未被明细覆盖的房间**补进来（避免漏房间，按房间去重故不重复）
- 跨月区间记录（如 7/20~8/05）→ 按结束日期归入对应月的快照，**不会被丢弃**（此项已在测试中修复并验证）

### 3.2 字段映射

复用现有 `server/parser.js` 的别名表，容错能力：

- 列名归一化：去空格、全半角括号统一、英文小写
- 别名示例：`总流水(元)` ← 总流水 / 流水(元) / 流水；`房间号` ← 直播间号 / 房间id / roomid
- 数值清洗：剥离 `元/天/小时/个/人/次/%/逗号` 后转数字，`-` 视为 0
- 日期：从「统计时间」列正则提取 `YYYY-MM-DD`；**文件若无该列，用接口参数 `statStart`/`statEnd` 兜底**
- 支持一个文件含多天（每行各读各的统计时间）

### 3.3 去重与幂等

- 同一次导入内：按去重键后者覆盖前者；重复行的「大航海人数」若为 0 而旧值非 0，保留旧值（避免 byDate 导出把已结算数据清零）
- 跨次导入：整月快照**整月覆盖**，重复导入同一文件条数与流水完全不变（已测试验证）
- 按日明细：走 `mergeRecords`，同 `房间号::日期` 覆盖，不影响其他日期

### 3.4 异常与缺失兜底规则

| 情况 | 处理 | 计数项 |
|---|---|---|
| 缺房间号 | **丢弃该行**，记入 errors | `missingRoom` |
| 缺运营 | 归 `未分配` | `missingOperator` |
| 缺统计日期 | 用 `statStart`/`statEnd` 兜底；仍无则用目标月最后一天 | `missingDate` |
| 统计区间跨月 | 按**结束日期**归入所属月，并在 warnings 列出 | `crossMonth` |
| 流水为负 | **保留原值**（可能是退款/冲正），仅告警 | `negativeFlow` |
| 归属表命中 | 覆盖导出数据里的运营字段 | `assignmentApplied` |
| 大航海为 0 | 若旧值非 0 则保留旧值 | — |

### 3.5 接口

```
POST   /api/month-import           导入（filePath 或 fileBase64；dryRun=1 只预检不落盘）
GET    /api/month-import/verify?month=2026-07    三口径对账
GET    /api/month-import/snapshots  已导入的快照概览
DELETE /api/month-import            删除某月（includeDaily=1 连按日记录一起删）
POST   /api/backfill-month          按月逐日补抓（B站后台还能查到时可用）
GET/POST/DELETE /api/operator-assignment         归属关系表
POST   /api/operator-assignment/bulk            归属表批量导入
```

导入请求示例：

```bash
curl -X POST http://localhost:8787/api/month-import \
  -H "Content-Type: application/json" -H "x-admin-token: <token>" \
  -d '{"filePath":"C:\\Users\\Administrator\\Desktop\\主播数据_2026.7.1-2026.7.31.xlsx",
       "month":"2026-07","statStart":"2026-07-01","statEnd":"2026-07-31","dryRun":true}'
```

---

## 四、导入后的校验方式

`GET /api/month-import/verify?month=2026-07` 做**三口径交叉比对**，任一不一致即 FAIL：

1. **源导入基准**：解析后、未经任何统计规则过滤的原始合计（已随快照存下）
2. **明细逐条求和**：该月所有参与统计的记录逐条累加
3. **页面汇总值**：运营月度统计实际渲染的数字

输出示例（测试实测）：

```
[OK ] 明细记录数: 8 vs 8
[OK ] 流水合计: 11550.75 vs 11550.75
[OK ] 运营「林夕」流水: 1000.5 vs 1000.5
[OK ] 运营「小辫子」流水: 1900 vs 1900
...
verify.ok = true
```

人工复核建议：把 B 站后台 7 月报表的**总计流水**与 `/api/operator-monthly-stats` 中 2026-07 各行 `totalFlow` 之和比对，两者应一致（分位误差 < 0.01）。

---

## 五、现有结构无法支持的场景与替代方案

### 5.1 整月区间导出无法还原「月内换运营」

**原因**：整月文件里一个主播只有一行、运营取导出时刻的当前值，流水也是整月合计，物理上没有变更前后的分界点。

**替代方案（按推荐度排序）**：

1. **改用按日补抓**（推荐）：`POST /api/backfill-month` 传 `month=2026-07`，系统自动跑 7/1~7/31 共 31 天。
   前提：B 站后台仍允许查询 7 月（今天距 7/1 约 68 天）。**这是唯一能完整还原归属变更的方案**，且数据进 latest.json，与 8/9 月同口径。
2. **归属表手工拆分**：若你手上有 7 月的变更记录（谁、什么时候、从哪个运营转到哪个运营），录入 `operator_assignment.json`；导入时按房间+日期覆盖归属。
   注意：这只能改**归属**，改不了流水拆分——整月流水仍会整块归给变更后的那个运营。
3. **接受整月归属**：按导出时的归属整块计入，页面中该月标注为「整月快照」，提醒复盘时留意。

### 5.2 线下名册没有历史维度

你选择**沿用当前 49 人名册**。风险：7 月在职、8 月离职且已移出名册的主播，其 7 月流水会计入「线上」。

需要精确时：提供 7 月在职的线下名单，通过导入参数 `offlineRooms: [...]` 传入，即可按 7 月名册判定（无需改代码）。

### 5.3 后台在册状态

流水统计不依赖在册状态，只要记录在系统里就出数。唯一要避免的是对历史月份执行「强制移除」（会真删记录）。`removed_rooms.json` 里的记录是「疑似移出」的留档，不影响统计。

---

## 六、落地清单

已完成：

- [x] `server/monthlySnapshot.js` — 月度快照读写（整月覆盖、幂等）
- [x] `server/operatorAssignment.js` — 归属关系表（生效/失效时间、按日期解析）
- [x] `server/monthImport.js` — 导入主逻辑（解析→归因→固化→去重→分流落盘）与 `verifyMonth` 对账
- [x] `server/operatorStats.js` — 双源融合 + 快照字段优先 + 混合模式补房间
- [x] `server/index.js` — 8 个新路由
- [x] 前端月份下拉标注数据来源（「2026-07 · 整月快照」）
- [x] 端到端测试通过（跨月不丢数、对账一致、幂等、归属覆盖、线下标记、异常兜底）

待你操作：

1. **重启服务** —— 新路由需重启才生效：双击 `重启守护服务.bat`（管理员身份）
2. **导入 7 月文件** —— 先用 `dryRun:true` 预检，确认条数/流水/警告无误后正式导入
3. **校验** —— `GET /api/month-import/verify?month=2026-07`，并比对 B 站后台 7 月总计
