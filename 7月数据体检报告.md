# 7 月数据文件体检报告

- 文件：`C:\Users\Administrator\Desktop\AI虚拟皮套\主播数据_2026-09-07 18_45_10.xlsx`
- 大小：6.25 MB｜格式：xlsx（zip 二进制，魔数 `50 4B 03 04`）｜编码：无风险（中文在 zip 内以 UTF-8 存储，读盘无损）
- 体检时间：2026-09-07

---

## 一、基础结论

| 项目 | 结果 |
|---|---|
| 能否正常读取 | ✅ 可以，xlsx 解析无异常 |
| 工作表数量 | 1 个，名称「主播数据」，范围 A1:BB31676 |
| 表头行 | 第 1 行（无前置标题行/合并单元格） |
| 列名数 | 54 列 |
| 数据行数 | **31,675 行** |
| 日期维度 | ✅ **按日明细**（统计时间 = 单日 `00:00:00 ~ 23:59:59`） |
| 实际统计范围 | **2026-07-01 ~ 2026-07-31，31 天全覆盖，无缺失、无跨月** |
| 唯一房间号 | 1,033 个（唯一昵称 785 个） |
| 重复键（房间号::日期） | **0 条** — 可直接幂等导入 |
| 总流水 | **¥746,137.60** |

## 二、文件名日期 vs 数据日期

**不一致，以文件内日期字段为准。**

- 文件名 `主播数据_2026-09-07 18_45_10` 中的 `2026-09-07 18:45:10` 是**导出时间**（今天下午生成）
- 文件内 `统计时间` 列的 31 个唯一值全部落在 2026-07-01 ~ 2026-07-31
- 逐日行数 1005 → 1033 递增（月初少、月末多，符合新增主播累积规律），无任何非 7 月日期

结论：这是一份**导出于 9/7 的 7 月按日明细完整数据**，可放心按 2026-07 导入。

## 三、关键指标字段

| 字段 | 类型 | 说明 |
|---|---|---|
| `总流水（元）` | 数值 | 核心指标，合计 ¥746,137.60，无负数、无缺失 |
| `总收益（元）` / `主播自提礼物收益` | 数值 | 完整 |
| `开播时长（小时）` | 数值 | 如 7.16 |
| `开播天数` | 字符串→数值 | 「1天」「0天」格式，解析器自动转数字 |
| `付费人数` / `粉丝数` / `新增粉丝数` / `弹幕数` | 数值 | 完整 |
| `峰值/平均在线人数（pcu/acu）` | 数值 | 完整 |
| `运营经纪人` | 字符串 | 「林夕\|运营组\|虚拟线下部」格式，取 `\|` 首段 |
| `大航海人数` | **全部为 `"-"`，无有效值** | 见异常项 A |

## 四、异常与缺失（4 项）

### A. 大航海人数 100% 为 `"-"`（**需关注**）

- 31,675 行全部是 `-`，0 行有数值
- 对照：8 月在库数据有 1,650 行（5.1%）大航海 > 0，最大值 38
- 原因：B 站「按日汇总」导出不含大航海字段（系统内已有注释记录此现象）
- 影响：导入后 **7 月大航海恒为 0**，运营月度统计页该指标环比会显示 -100%
- 兜底：解析器 `toNumber('-') = 0`；`mergeRecords` 有「新值为 0 且旧值非零则保留旧值」的保护，不会污染已有数据

### B. 开播分区 95% 为空（**正常**）

- 30,092 / 31,675 行为空
- 对照：8 月在库同样只有 5.0% 非空 → 该字段仅在当日有开播时才填充，属正常现象，非缺失

### C. 重复列名（**已确认无影响**）

- `主播身份（是否新主播）` 出现在第 3、10 列
- `主播在会时间` 出现在第 9、11 列
- 解析器 `buildHeaderMap` 有「同一标准列只映射一次」的守卫，且两列取值完全一致，不会互相覆盖

### D. 19 个列不在系统 schema 内，导入时会被丢弃

`招募经纪人`、`招募经纪人UID`、`语聊房嘉宾开播时长（小时）`、`人均观看时长`、`有效观看时长`、`人均有效观看时长`、`曝光人数`、`进房人数`、`进房次数`、`累计发布稿件数`、`有效视频投稿量`、`有效稿件播放量`、`累计有效稿件播放量`、`付费率`、`发布动态数量`、`渠道来源备注`、`主播身份备注`，以及两个重复列的副本

> 与现有 37 字段 schema 保持一致（8 月在库记录也是 37 键）。已验证「语聊房嘉宾开播时长」不会误覆盖「开播时长」——别名守卫生效。若后续需要 `进房人数`/`曝光人数` 做漏斗分析，需先扩 schema。

## 五、口径一致性检查（7 月 vs 8 月在库）

| 对比项 | 7 月文件 | 8 月在库 | 结论 |
|---|---|---|---|
| 房间集合 | 1,033 | 1,045 | 7 月集合 **100% 被 8 月包含**（8 月多 12 个新房间） |
| 运营部门后缀 | 100% 虚拟线下部 | 100% 虚拟线下部 | 一致 |
| 运营构成 | 林夕/梧桐/予我/小辫子/轨迹 | 同左 5 人 | 一致 |
| 有流水房间数 | 113 | 122 | 同量级 |
| 线下名册内流水占比 | 18.7% | 24.2% | 同量级（名册仅 49 人，本就只覆盖一部分，属既有口径，非 7 月文件问题） |

**源口径 7 月各运营流水：**

| 运营 | 行数 | 房间数 | 总流水 |
|---|---|---|---|
| 梧桐 | 5,713 | 189 | ¥371,757.70 |
| 林夕 | 5,891 | 194 | ¥159,892.20 |
| 小辫子 | 805 | 27 | ¥114,913.80 |
| 予我 | 6,742 | 219 | ¥99,433.90 |
| 轨迹 | 12,524 | 404 | ¥140.00 |
| **合计** | **31,675** | **1,033** | **¥746,137.60** |

## 六、⚠️ 关键发现：导出文件回填了「当前归属」

### 现象

7 月文件里，**每个房间在 31 天中的运营归属完全恒定**（0 条月内变更）。对比在库数据：

| 对比基准 | 不一致房间数 |
|---|---|
| 7 月文件归属 vs **9/7（导出当天）**在库归属 | **0 个** |
| 7 月文件归属 vs **8/1** 在库归属 | **1 个** — 房间 `1732562810` |

### 决定性证据

房间 `1732562810`（月出skerry）：

- 在库归属序列：`2026-08-01 ~ 09-03 挂「轨迹」` → `2026-09-04 起转「小辫子」`
- 7 月文件里该房间归属写作 **小辫子**（= 导出当天的当前值）
- 招募经纪人字段为 **轨迹**（招募关系粘性强，不随运营变更而变）

经济合理性验证：

```
该房间 8 月流水        ¥35,389
轨迹 8 月总流水        ¥35,529    → 该房间占 99.6%
轨迹 8 月剔除该房间    ¥140       ← 与 7 月文件里「轨迹」的 ¥140 完全吻合
```

- 假设 A（回填）：轨迹 7 月真实值 = 140 + 25,757 = **¥25,897**，与 8 月 ¥35,529 同量级 ✅
- 假设 B（历史归属）：轨迹 7 月 = ¥140 → 8 月 ¥35,529，暴涨 254 倍 ❌

**结论：B 站的历史区间导出会把「当前归属」回填到所有历史行，而非保留当日归属。** 这与 8/9 月每日抓取的数据不同（每日抓取保留当日归属，已实测房间 1732562810 在 9/4 发生切换）。

### 影响面（已量化，可控）

- 受影响的只有 **1 个房间**：`1732562810`（月出skerry），7 月流水 **¥25,757**
- 占 7 月总量 3.45%
- 若按导出原样导入：`小辫子` 7 月 = ¥114,913.8（虚高）、`轨迹` 7 月 = ¥140（虚低）
- 修正后应为：`小辫子` = **¥89,156.8**、`轨迹` = **¥25,897**
- 对 `轨迹` 影响极大（185 倍），若 7 月要做 KPI 考核必须修正

### 残留风险（不可检出）

若某房间在 **7 月内**变更运营、且变更后一直维持到 9/7，则该房间的 7 月流水会被整月算给变更后的运营，从数据上无法检出。
参考变更频率：8/1~9/7 共 38 天、1,045 个房间仅发生 1 次变更，推算 7 月内发生变更的期望约 0~1 次，风险很低。

## 七、预检结果（dryRun，未写入）

```
mode = daily（按日明细，进 latest.json）
count = 31675   daily = 31675   range = 0
missingRoom = 0   missingDate = 0   missingOperator = 0
negativeFlow = 0   crossMonth = 0
warnings = 0      errors = 0
sourceTotals.totalFlow = 746137.6  ✅ 与独立核算完全一致
```

---

## 八、待确认事项

1. **是否为房间 `1732562810` 录入归属修正**（推荐：是）
   写入归属关系表 `operator_assignment.json`：`1732562810 → 轨迹`，生效区间覆盖 2026-07-01 ~ 2026-09-03，导入时自动覆盖导出值。
2. **7 月大航海恒为 0 是否接受**（该指标 7 月环比将显示 -100%）
3. 确认后执行正式导入，再跑 `/api/month-import/verify?month=2026-07` 三口径对账。
