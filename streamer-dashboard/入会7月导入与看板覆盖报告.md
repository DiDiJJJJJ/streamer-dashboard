# 入退会 7 月数据导入与看板覆盖报告

> 生成时间：2026-09-08 16:35｜操作人：AI 助手（Agent 模式）

## 一、任务
将桌面 `AI虚拟皮套/入会管理_2026-09-08 16_17_54.xlsx` 中的 **2026 年 7 月入退会数据** 与系统现有数据一并导入，并完整补齐看板中所有与入退会相关的统计、图表、指标，使其覆盖 7 月且保持一致。

## 二、数据源核对
- 文件含 **29 条**记录，统计时间均为 `2026-07-01 00:00:00 ~ 2026-07-31 23:59:59`（即整月 7 月）。
- 状态分布：**28 条「已入会」 + 1 条「已过期」**（扶光和他的猫）。
- 表头与系统 B站「入会管理」导出完全一致（主播昵称/UID/房间号/经纪人/合作时间/邀约人/邀约状态/时间点/统计时间），解析无需定制。

## 三、导入动作
- 文件已复制到 `server/downloads/recruit_1788857022_入会管理_2026-09-08 16_17_54.xlsx`。
- 复用现有 `runUnionRecruitStats({ dryRun, localFile })` 解析，按去重键 `uid|time|status` 合并进 `server/data/union_recruit_archive.json`（入退会**唯一权威去重库**）。
- 结果：archive 由 **28 → 57 条**，本次新增 **29 条**（0 冲突、0 重复）。`已过期` 记录入库但不计入入会数（与系统"仅统计已入会"口径一致）。

## 四、7 月入会数据（按运营）
| 运营 | 总入会 | 线上 | 线下 |
|---|---|---|---|
| 梧桐 | 12 | 3 | 9 |
| 林夕 | 9 | 3 | 6 |
| 予我 | 4 | 2 | 2 |
| 小辫子 | 3 | 0 | 3 |
| 轨迹 | 0 | 0 | 0 |
| **合计** | **28** | **8** | **20** |

## 五、看板覆盖逻辑（关键）
入会数的**唯一数据来源**是 `union_recruit_archive.json`：
- `jobs/unionRecruitStats.js`（入会招募统计页）直接读它；
- `operatorStats.js` 也读它，按「运营 × 自然月」聚合出 `joined / joinedOnline / joinedOffline`，供给 **运营月度统计 / 运营团队变动 / KPI 月度** 三个页面的「入会数」列。

因此，导入 7 月后**所有相关页面自动覆盖 7 月**，无需逐页改造。已独立验证：
- `operatorStats` 覆盖月份变为 **2026-07 / 2026-08 / 2026-09**；
- 7 月「入会数」合计 **28**（梧桐12/林夕9/予我4/小辫子3/轨迹0），与 recruit 口径**三方一致**；
- 7 月「总流水」也吻合此前 7 月流水导入（梧桐 ¥371,757.7 / 林夕 ¥159,892.2 / 予我 ¥99,433.9 / 小辫子 ¥89,157 / 轨迹 ¥25,896.8）—— 流水页与入会页在 7 月已打通。
- 月度趋势：07=28、08=16、09=8（连续 3 个月可见）。

## 六、本次代码改动
**后端**
- `server/jobs/unionRecruitStats.js`：`aggregateRecruitStats(range, month)` 新增 `month='YYYY-MM'` 过滤；输出 `availableMonths`（可用月份升序）与 `monthlyTrend`（每月 total/online/offline/unknown）。
- `server/index.js`：`GET /api/union-recruit-stats` 支持 `?month=YYYY-MM`。

**前端**
- `src/components/UnionRecruitPage.jsx`：新增「历史月份」下拉（来自 `availableMonths`，含 2026-07）+ **月度入会趋势堆叠柱状图**（recharts，覆盖全部历史月份，含 7 月）。

**构建**：`vite build` 成功，`vendor-charts`（recharts）已打入；两处 server 文件 `node --check` 通过。

## 七、需用户执行（使新代码生效）
1. **重启服务**：双击 `启动主播管理后台服务-自动重启.bat`（**管理员**）。原因：新增的 `?month=` 接口与重写的 `aggregateRecruitStats` 需载入新 server 代码；前端 `dist/` 已落盘，静态资源即时生效，但 API 逻辑依赖重启。
2. **强刷浏览器**：在「入会招募统计」「运营月度统计」页按 `Ctrl+Shift+R`。
3. 验证点：
   - 入会招募统计页 → 顶部「历史月份」选 **2026-07** → 汇总卡片=28 人，明细表按运营展开，趋势图出现 7 月柱；
   - 运营月度统计页 → 月份列已含 **2026-07**，各运营「入会数/线上入会/线下入会」已填充。

## 八、口径说明
- 「已过期/已拒绝/待审批」状态不计入入会数（仅入库）；
- 线上/线下按**当前** `roster.json` 的 `offlineRooms` 房间号判定，与系统既有口径一致；
- 7 月已按用户此前要求**不纳入 KPI 考核**（`config.kpiExcludedMonths` 含 `2026-07`），但不影响「运营月度统计」页展示与环比。
